# [COCOSOLIS](https://cocosolis.com/en/) visual copy

To run it:

Install the dependencies in the root and frontend directories:

```bash
pnpm install

cd frontend; pnpm install
```

Then run nuxt:

```bash
# npm
cd frontend; pnpm run dev
```

Open `http://localhost:3000`
